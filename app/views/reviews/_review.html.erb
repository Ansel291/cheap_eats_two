<p><%= review.rating %></p>
<p><%= review.comment %></p>

<%= link_to "Edit", edit_dish_review_path(review.dish, review) %>
<%= link_to "Delete", dish_review_path(review.dish, review), method: :delete, data: { confirm: "Are you sure?" } %>
